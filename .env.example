# NextAuth Configuration
# Generate a secret: openssl rand -base64 32
NEXTAUTH_SECRET=your-nextauth-secret-here
NEXTAUTH_URL=http://localhost:3000

# Public Configuration
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Development Configuration
NODE_ENV=development
PORT=3000

# Google Sign In
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret

# Azure SQL (MSSQL) for Kysely
MSSQL_SERVER=your-server.database.windows.net
MSSQL_DATABASE=your_db
MSSQL_USER=your_user
MSSQL_PASSWORD=your_password
# Azure SQL requires encryption
MSSQL_ENCRYPT=true
# Connection pool sizing (optional)
MSSQL_POOL_MIN=0
MSSQL_POOL_MAX=10

# Stripe Configuration
# Get these from your Stripe dashboard (use test mode for development)
STRIPE_SECRET_KEY=sk_test_your-stripe-secret-key
STRIPE_WEBHOOK_SECRET=whsec_your-webhook-secret
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your-publishable-key

# Stripe Product IDs
STRIPE_SUBSCRIPTION_ID_BASIC=prod_your_product_id
STRIPE_SUBSCRIPTION_ID_PREMIUM=prod_your_product_id

# AI Provider Configuration
# Set to "azure" for Azure OpenAI or "openai" for OpenAI
AI_PROVIDER=azure

# Azure OpenAI Configuration (if using Azure OpenAI)
AZURE_OPENAI_API_KEY=your-azure-openai-api-key
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com
AZURE_OPENAI_DEPLOYMENT=gpt-4o-mini

# OpenAI Configuration (if using OpenAI directly)
OPENAI_API_KEY=your-openai-api-key

# Azure Storage Configuration
AZURE_STORAGE_CONNECTION_STRING=DefaultEndpointsProtocol=https;AccountName=your-account;AccountKey=your-key;EndpointSuffix=core.windows.net

# Azure Web PubSub Configuration (for real-time features)
AZURE_WEB_PUBSUB_CONNECTION_STRING=Endpoint=https://your-webpubsub.webpubsub.azure.com;AccessKey=your-access-key;Version=1.0;

# Application Insights Configuration
APPINSIGHTS_CONNECTION_STRING=InstrumentationKey=your-instrumentation-key;IngestionEndpoint=https://your-region.in.applicationinsights.azure.com/